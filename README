This project contains the unfinished implementation of a so-called Fully Convolutional Network to predict pixel-level class labels.
In the example case we use a dataset containing only a single image showing an urban landscape. The two classes are 'house' and 'not a house'.

